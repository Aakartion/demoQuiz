spring:
  application:
    name: demo

  datasource:
    url: jdbc:mysql://localhost:3306/demoQuiz
    username: root
    password: 12345

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
